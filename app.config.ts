export default defineAppConfig({
    ui: {
      notifications: {
        /**
         * Show toasts at the top right of the screen
         * @see https://ui.nuxt.com/components/notification#usage
         */
        position: 'top-0 bottom-[unset] left-[unset] right-0'
      }
    }
  })
  